# TFCNNv2.1
This version uses derivatives w.r.t x rather than w.r.t f(x) which is what TFCNNv2 does. As a result this version has a slightly higher memory usage overhead.

This is work-in-progress (WIP).
